version: '3.8'

services:
  docling-service:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: docling-service
    ports:
      - "8080:8080"
    environment:
    PORT=8080
    DOCLING_API_KEY=dlk_live_3Qp7yKfZVx9wJ2N5tH0c

  # EAN Search API
  EAN_SEARCH_API_KEY=122c4a2c6d8d07b2425746d33b61a6637eebd492
  EAN_SEARCH_API_URL=https://api.ean-search.org/api

  # OpenAI API (for brand voice)
  OPENAI_API_KEY=OPENAI_API_KEY=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
  OPENAI_MODEL=gpt-4o-mini

  # OCR Settings
  ENABLE_OCR=true
  OCR_ENGINE=tesseract

      # ADD THESE TIMEOUT SETTINGS
      TIMEOUT_KEEP_ALIVE: "650"  # 10 minutes + buffer
      TIMEOUT_GRACEFUL_SHUTDOWN: "30"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:8080/healthz"]
      interval: 30s
      timeout: 5s
      retries: 5

      # Server Configuration
      - PORT=8080
      - HOST=0.0.0.0
      - LOG_LEVEL=info

      # Service Configuration
      - MAX_FILE_SIZE_MB=50
      - ALLOWED_EXTENSIONS=pdf,docx,xlsx,pptx,html,txt
      - TEMP_DIR=/tmp/docling

      # Processing Configuration
      - MAX_CONCURRENT_JOBS=5
      - ENABLE_OCR=true
      - OCR_LANGUAGE=eng

      # CORS Configuration
      - CORS_ORIGINS=*
      - CORS_ALLOW_CREDENTIALS=true
      - CORS_ALLOW_METHODS=*
      - CORS_ALLOW_HEADERS=*

    volumes:
      # Mount temp directory for document processing
      - docling-temp:/tmp/docling
      # Optional: mount local .env file
      # - ./.env:/app/.env

    restart: unless-stopped

    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/healthz"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

    # Resource limits (adjust based on your needs)
    deploy:
      resources:
        limits:
          cpus: '2.0'
          memory: 4G
        reservations:
          cpus: '1.0'
          memory: 2G

volumes:
  docling-temp:
    driver: local
